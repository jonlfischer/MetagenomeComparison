package edu.metagenomenewtest.presenter;

import javafx.scene.Group;
import javafx.scene.Node;
import javafx.scene.control.Tab;
import javafx.scene.layout.Background;
import javafx.scene.layout.Pane;
import javafx.scene.paint.Color;
import javafx.stage.Stage;
import jloda.fx.dialog.ExportImageDialog;
import jloda.graph.Graph;

public class Export {

    /**
     * export a graph with open tabs. When you want to continue displaying, the representation and tab content as to be
     * drawn newly after using this function!
     * @param representation
     * @param tabs
     */
    public static void exportMainWithPairwiseTabs(GraphRepresentation representation, GraphTab[][] tabs){

    }

    /**
     * export a graph with open tabs. When you want to continue displaying, the representation and tab content as to be
     * drawn newly after using this function!
     * @param representation
     * @param tabs
     */
    public static void exportMainWithSingleTabs(GraphRepresentation representation, GraphTab[] tabs){

    }


}
